import 'package:flutter/material.dart';

// TODO: Implement Figma: Bank Account > Find your bank (searchable list)
class FindBankScreen extends StatelessWidget {
  const FindBankScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('FindBankScreen')),
    );
  }
}