import 'package:flutter/material.dart';

// TODO: Implement Figma: Earnings > Earnings Status detail
class EarningsStatusScreen extends StatelessWidget {
  const EarningsStatusScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('EarningsStatusScreen')),
    );
  }
}