import 'package:flutter/material.dart';

// TODO: Implement Figma: Log in with Rider's ID
class LoginRiderIdScreen extends StatelessWidget {
  const LoginRiderIdScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('LoginRiderIdScreen')),
    );
  }
}